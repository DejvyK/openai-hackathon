Continue local media production. Own only scripts/production/video/** and docs/production/video/**. You are not alone; preserve other edits. No app changes, providers, network, installs, publishing or new agents. User authorizes Codex CLI local production and iteration.

Problem: current export verifies against a live changing handoff. Do not falsify original hashes or weaken freshness checks to pretend it is current. Implement an explicit immutable source snapshot so the local export is reproducible independently of future source changes. Capture only the existing 13 allowlisted text/SVG sources used by build.mjs; preserve original bytes and hashes (no secrets or broader repo capture). Fail if inputs change while capturing. Preserve snapshot provenance/time. Build must read snapshot bytes, validate hashes and render explicit snapshot/evidence labeling; fail if narration claims conflict with accepted J1/J2 in that snapshot. A clearly named refresh option can create a new snapshot after review; ordinary rebuilds use the existing snapshot. Record snapshot hash in final manifest. Do not claim toolchain-independent identical MP4 bytes.

Verifier must distinguish internal integrity (snapshot, scripts, excerpts, media) from current-worktree freshness. Check actual current MP4 metadata via ffprobe and full decode, not only a stored receipt. No self-rewriting manifest/hashes during verification. Leave a separate verification receipt. Freshness drift must be visible and a strict --require-current check must fail if source files differ or are missing; default historical artifact integrity can pass with explicitly reported stale state. Verify all quoted excerpts are present in their original snapshot bytes and actual used line refs. Test corruption handling with temp copies, without editing real source inputs or final files. Do not just special-case delivery-plan changes.

Regenerate the actual 120s video from the captured snapshot; inspect contact sheet and final files. Update README with reproduction, freshness semantics, exact checks, and limitations including synthetic narration/manual listening still pending. Keep the artifact a historical evidence review, not a live browser demo. End with commands and outcomes. Do not modify parent ACCEPTANCE or root docs.
