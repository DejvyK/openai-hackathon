# Produce the actual local evidence walkthrough

User authorizes local artifact production and iteration using Codex CLI. This is documentation/media work, not app implementation or publication. You are not alone in this checkout: preserve other edits. Own ONLY `docs/production/video/**` and `scripts/production/video/**`. Do not modify app code, root manifests, existing story files, shared plans, credentials, or provider state. Do not start additional agents. No network, installs, uploads, social posts or workspace writes.

Read `docs/production/BRIEFS.md`, `docs/production/story/VOICEOVER.md`, `PITCH.md` and the actual referenced evidence. Build a real 120.000-second MP4 evidence walkthrough using available local ffmpeg/ffprobe, Playwright Chromium and Windows System.Speech if a suitable English voice exists. This is an evidence review, not a live product recording. Keep that qualification visible throughout. Do not simulate a successful app, fabricate screenshots or synthesize provider results.

Create a polished 1600x900 sequence with readable actual excerpts from the named repository files. Preserve exact source excerpts and label filenames; retain source hashes in a manifest. Report-derived summaries must be labeled summaries. Use 7 windows from the existing script. Do not shrink whole documents to unreadability. Maintain visual consistency with story/POSTER.svg. Use no external assets/fonts. Do not include secrets or raw private logs. Use a stock English synthesized narrator if available, clearly disclose synthetic narration, and replace first-person David wording with third-person author credit (do not impersonate David). Render open captions or readable narration captions. Narration audio must fit windows without cropping speech; measure duration and adjust speaking rate or wording responsibly. If TTS is unavailable, deliver a clearly labeled captioned silent rough cut and report the blocker rather than falsely claiming voiced video.

Deliver `docs/production/video/AgentLayer-evidence-review.mp4`, a poster/contact-sheet PNG for review, `README.md` with reproduction and honest limits, and `manifest.json` with actual file hashes, durations, evidence sources and generated-media attribution. Keep large intermediate files in ignored `.agentlayer/production/video/`, final artifacts in owned directory. Put repeatable scripts in `scripts/production/video/`. Verify export duration, codecs, full decode and frames around all seven shot boundaries with ffprobe/ffmpeg. Inspect a contact sheet through view_image. No final publication or product acceptance claim. If source state contradicts narration, fix narration to current evidence. Finish with exact checks, outputs and unresolved limitations.
